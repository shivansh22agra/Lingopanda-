import 'package:flutter/material.dart';

class LikePage extends StatelessWidget {
  static String id="/likepage";
  const LikePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('like')),
    );
  }
}